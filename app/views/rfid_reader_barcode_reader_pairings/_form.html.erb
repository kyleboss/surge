<%= form_for(@rfid_reader_barcode_reader_pairing) do |f| %>
  <% if @rfid_reader_barcode_reader_pairing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rfid_reader_barcode_reader_pairing.errors.count, "error") %> prohibited this rfid_reader_barcode_reader_pairing from being saved:</h2>

      <ul>
      <% @rfid_reader_barcode_reader_pairing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :barcode_reader_id %><br>
    <%= f.text_field :barcode_reader_id %>
  </div>
  <div class="field">
    <%= f.label :rfid_reader_id %><br>
    <%= f.text_field :rfid_reader_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
