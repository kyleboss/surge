<%= form_for(@rfid_reader) do |f| %>
  <% if @rfid_reader.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rfid_reader.errors.count, "error") %> prohibited this rfid_reader from being saved:</h2>

      <ul>
      <% @rfid_reader.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reader_id %><br>
    <%= f.text_field :reader_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
