<%= form_for(@rfid_scan) do |f| %>
  <% if @rfid_scan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rfid_scan.errors.count, "error") %> prohibited this rfid_scan from being saved:</h2>

      <ul>
      <% @rfid_scan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rfid_reader_id %><br>
    <%= f.text_field :rfid_reader_id %>
  </div>
  <div class="field">
    <%= f.label :rfid_id %><br>
    <%= f.text_field :rfid_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
