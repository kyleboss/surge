I"¥(function() {
  var boxClicked, iClicked, inventorySolutionBox, locationSolutionBox, solutionBoxes, timeSolutionBox;

  window.ready = ((function() {
    var i, len, results;
    (function() {});
    inventorySolutionBox = document.getElementById('inventory-solution');
    timeSolutionBox = document.getElementById('time-solution');
    locationSolutionBox = document.getElementById('location-solution');
    solutionBoxes = [inventorySolutionBox, timeSolutionBox, locationSolutionBox];
    results = [];
    for (iClicked = i = 0, len = solutionBoxes.length; i < len; iClicked = ++i) {
      boxClicked = solutionBoxes[iClicked];
      results.push(boxClicked.addEventListener('click', function() {
        var boxUnselect, iUnselect, j, len1;
        alert("hello");
        for (iUnselect = j = 0, len1 = solutionBoxes.length; j < len1; iUnselect = ++j) {
          boxUnselect = solutionBoxes[iUnselect];
          if (boxUnselect !== boxClicked) {
            boxUnselect.classList.remove('selected-solution-box');
          } else {
            boxClicked.classList.add('selected-solution-box');
          }
        }
      }));
    }
    return results;
  })())();

}).call(this);
:ET